<p style="color: green"><%= notice %></p>
<%= "最近登録の部屋は、#{session[:room]}です。" if session[:room] %>
<h1>Rooms</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>list
      <%= link_to "Show this room", room %>
    </p>
  <% end %>
</div>

<%= link_to "New room", new_room_path %>
